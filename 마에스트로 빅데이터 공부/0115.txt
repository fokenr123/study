지도학습  vs  비지도 학습 -종속변수 여부

비지도 학습 - 차원 축소 , target 결정

vector matrix  - 데이터 변환, 차원의 축소 -> 특징을 추출 
vector 선형대수
- vector 크기 -knn , k-mean  
- 방향 -PCA, MDS  (내적) FFNM 
A * B = (A||B | cos x)  cos 유사도 ?? - 내적유사도 - 찾아보기 
center 점 을 정해줌
pca : 상관계수나 , 공분산 구함
상관계수가 정방행렬 이면서 대칭행렬을 이룸
고유값 분해 - >  고유값 + 고유벡터 -> 내적이 0 : 의미는 정직교한다 
고유값 분해시 85%까지 설명이 되면 위에 2개가 주성분 분석이 된다 

pca는 모델의 전 단계에서 사용됨 

mds : 만들어 놓은 결과가 분석용으로는 부적합 ( 시각화용 )

50 x 10 은 10차원이기 때문에 시각화를 할수 없기 때문에 임의의 행렬을 10 * 2 사용 50 * 2 = > 2차원으로 시각화 할수 있음 (재해석)
FFNM : mds 같은 방식이나 ffnm은 내일 설명 
y- y^ 오차함수 : 포물선을 띄는 형태로 가중치를 추정하고 학습율을 낮춘다 

k-means : 
문제점 
중심값을 정하는게 , 군집수를 결정, 이상치의 영향을 받음 
활용 : 1. 종속변수 결정 (비지도학습이므로 종속변수를 만들때 사용 )
        2. 압축용도  - palatte 
        3. 원형이상치 제거 - 중심값에서 일정한 범위 밖에 있는 값을 제거 할 수 있음 (이상치) knn 
